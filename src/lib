import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://SEU-PROJETO.supabase.co'; // coloque seu URL do Supabase aqui
const supabaseAnonKey = 'SUA-CHAVE-ANON'; // coloque sua chave anon aqui

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
